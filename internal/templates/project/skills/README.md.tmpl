# skills/ — project-specific skills for {{.Name}}

Reusable instructions or sub-recipes scoped to this project. In the MVP you add
these by hand; a future version (see V2 §2) adds `bake skill add` to manage them.

Reference a skill from `recipe.yaml` (as a sub-recipe) or from `.goosehints`.
