# docs/ — curated reference for {{.Name}}

Drop reference material here (notes, specs, exported PDFs as text/markdown). The
assistant reads these on demand. Keep it curated — this is a reference shelf, not
a dumping ground.
